# Pin THIS project's Gradle daemon to JDK 21, independent of the machine JAVA_HOME
# (which stays on JDK 11 for Better Monster Examine's Gradle 7.4 build).
# Gradle 9.5.1 requires Java 17+ to run, and Spring Boot 4 targets Java 21.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot

org.gradle.caching=true
org.gradle.parallel=true
